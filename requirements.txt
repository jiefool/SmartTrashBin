# Core dependency (pin first so everything else aligns)
numpy==1.26.4

# Web / API
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Scheduling (periodic fill-level checks)
apscheduler==3.10.4

# Environment / config
python-dotenv==1.0.1

# Logging
loguru==0.7.2

# Image classification (TrashNet)
# On Mac (training): pip install tensorflow>=2.15.0
# On Raspberry Pi (inference only): pip install tflite-runtime
tflite-runtime>=2.14.0; platform_machine == 'aarch64'
tensorflow>=2.15.0; platform_machine != 'aarch64'
Pillow==10.3.0
scikit-learn==1.4.2
scipy==1.12.0
h5py==3.10.0
opencv-python==4.9.0.80

# Camera (Raspberry Pi)
# picamera2 is pre-installed on Raspberry Pi OS – do not pip install it
# If using USB webcam, opencv-python above handles it

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

